// Utility function to map WooCommerce product to PC component format
// This is kept for compatibility but the main mapping happens in syncService

export function mapWooCommerceToPCComponent(product: any): any {
  // This is a placeholder - actual mapping happens during sync
  return product;
}
